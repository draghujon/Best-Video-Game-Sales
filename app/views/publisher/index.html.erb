<h4 class="subtitle is-4">Publisher</h4>
<table class="table is-1">
 <tbody>
    <% @publishers.each do |pub| %>
      <tr>
        <td><%= link_to pub.pub_name, pub %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'Back', game_index_path %>